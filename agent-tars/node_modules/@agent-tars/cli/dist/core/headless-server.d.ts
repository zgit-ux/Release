import http from 'http';
import { AgentTARSAppConfig } from '@agent-tars/interface';
interface HeadlessServerOptions {
    appConfig: AgentTARSAppConfig;
    isDebug?: boolean;
}
/**
 * Start the Agent TARS server in headless mode (API only, no UI)
 */
export declare function startHeadlessServer(options: HeadlessServerOptions): Promise<http.Server>;
export {};
//# sourceMappingURL=headless-server.d.ts.map