/**
 * Process the request command, sending a raw request to the LLM provider
 */
export declare function processRequestCommand(options: {
    provider: string;
    model: string;
    body: string;
    apiKey?: string;
    baseURL?: string;
    stream?: boolean;
    thinking?: boolean;
    format?: 'raw' | 'semantic';
}): Promise<void>;
//# sourceMappingURL=request.d.ts.map