/**
 * Options for LLM request
 */
export interface LLMRequestOptions {
    /**
     * Provider name
     */
    provider: string;
    /**
     * Model name
     */
    model: string;
    /**
     * Path to the request body JSON file or JSON string
     */
    body: string;
    /**
     * API key (optional)
     */
    apiKey?: string;
    /**
     * Base URL (optional)
     */
    baseURL?: string;
    /**
     * Whether to use streaming mode
     */
    stream?: boolean;
    /**
     * Whether to use thinking mode
     */
    thinking?: boolean;
}
/**
 * A standalone module to send requests to LLM providers without creating a full Agent
 */
export declare class LLMRequester {
    /**
     * Send a request to LLM provider
     */
    request(options: LLMRequestOptions): Promise<any>;
    /**
     * Parse the request body from a file path or JSON string
     */
    private getRequestBody;
}
//# sourceMappingURL=llm-requester.d.ts.map