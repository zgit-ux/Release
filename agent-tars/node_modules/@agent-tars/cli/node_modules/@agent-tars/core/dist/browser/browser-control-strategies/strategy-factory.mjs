/**
* Copyright (c) 2025 Bytedance, Inc. and its affiliates.
* SPDX-License-Identifier: Apache-2.0
*/
import { MixedControlStrategy } from "./mixed-strategy.mjs";
import { GUIAgentOnlyStrategy } from "./gui-agent-only-strategy.mjs";
import { BrowserUseOnlyStrategy } from "./browser-use-only-strategy.mjs";
class StrategyFactory {
    static createStrategy(mode, logger) {
        switch(mode){
            case 'mixed':
                return new MixedControlStrategy(logger);
            case 'gui-agent-only':
                return new GUIAgentOnlyStrategy(logger);
            case 'browser-use-only':
                return new BrowserUseOnlyStrategy(logger);
            default:
                logger.warn(`Unknown browser control mode: ${mode}, falling back to default`);
                return new MixedControlStrategy(logger);
        }
    }
}
export { StrategyFactory };

//# sourceMappingURL=strategy-factory.mjs.map