/**
* Copyright (c) 2025 Bytedance, Inc. and its affiliates.
* SPDX-License-Identifier: Apache-2.0
*/
import external_express_default from "express";
import external_cors_default from "cors";
import { registerAllRoutes } from "./routes/index.mjs";
function getDefaultCorsOptions() {
    return {
        origin: '*',
        methods: [
            'GET',
            'POST',
            'PUT',
            'DELETE',
            'OPTIONS'
        ],
        allowedHeaders: [
            'Content-Type',
            'Authorization'
        ]
    };
}
function setupAPI(app) {
    app.use(external_cors_default(getDefaultCorsOptions()));
    app.use(external_express_default.json({
        limit: '20mb'
    }));
    registerAllRoutes(app);
}
export { getDefaultCorsOptions, setupAPI };

//# sourceMappingURL=index.mjs.map